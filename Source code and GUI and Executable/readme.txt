The GUI called demo.py

To run the demo, put the demo.py file in the same folder with lex.l and yacc.y files, 
and with each file needed to be called inside the GUI.
then open cmd and go to the directory of the demo, then type 'python demo.py'

Put the testcase in GUI and will output two files which are compiledoutput.txt and symboltable.txt